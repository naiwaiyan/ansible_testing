---

- hosts: all
  become: true
  tasks:

  - name: install bind9
    apt:
      name: bind9
      state: latest

  - name: update apt
    apt:
      update_cache: yes
    when: ansible_distribution == " Cent "

  - name: install apache2 package
    apt:
      name: apache2
      state: latest
